export const metadata = {
  title: "SPX Strategy Command v2",
  description: "Multi-Strategy SPX Options Dashboard — AI Signals, Greeks, OI/GEX",
};
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, background: "#0a0e1a" }}>{children}</body>
    </html>
  );
}
